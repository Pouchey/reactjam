import { TGameState } from '_types/game';
import { TRoundAction } from '_types/round';

export const playRound = (
  game: TGameState,
  playerId: string,
  action: TRoundAction
) => {
  // TODO
  action;
  playerId;
  game;
};
